# cpp-secure-banking-system
A simple console based banking system built with C++. It supports user account creation, login authentication, and basic transactions (deposit, withdraw, balance check). User credentials are stored in a local file-based database with hashed passwords and PINs using the DJB2 algorithm for basic security.
